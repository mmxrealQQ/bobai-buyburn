node_modules/
.env
.env.*.backup
temp/
rebranding/
assets/
!dashboard/worldcup/app/assets/
!dashboard/brainscreener/assets/
.wrangler/

# Local output dumps & ephemeral logs (no production value)
worldcup/
wrangler-tail.log
*.log

# Internal-only — never push (whale tracker scratchpads & private refs)
scan-whales.mjs
ref1.jpg

# WC payout snapshot — contains username→wallet→balance→rank (privacy)
wc-payout-snapshot.json
wc-payout-final-snapshot.json

# internal notes & drafts (local only)
x-posts/
.claude/
databrain/
tg-update-*.txt
bscscan-nft-submission.txt
element-collection-submission.txt
bob-buyback.sh
deploy-bobai.sh
