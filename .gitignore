node_modules/
.env
.env.*.backup
temp/
rebranding/
assets/
!dashboard/worldcup/app/assets/
!dashboard/brainscreener/assets/
.wrangler/

# Local output dumps & ephemeral logs (no production value)
worldcup/
wrangler-tail.log
*.log

# Internal-only — never push (whale tracker scratchpads & private refs)
scan-whales.mjs
ref1.jpg

# WC payout snapshot — contains username→wallet→balance→rank (privacy)
wc-payout-snapshot.json
wc-payout-final-snapshot.json

# internal notes & drafts (local only)
x-posts/
.claude/
databrain/
tg-update-*.txt
bscscan-nft-submission.txt
element-collection-submission.txt
bob-buyback.sh
deploy-bobai.sh
bobai-fairytale*
giphy-rating-mail.txt
x-*post*.txt
hello-bobai-meme.jpg

# Census working files — the scan output is large and regenerable;
# the published result lives in dashboard/api-registry.json instead.
data/erc8004-v2/agents-with-endpoints.jsonl
data/erc8004-v2/offchain-uris.jsonl
data/erc8004-v2/reachable.jsonl
data-scan.log
burn-entry.json

# B402 partner keypair — the private half must never be pushed
secrets/

# The raw job scan is 23 MB and fully regenerable; the aggregate that the
# page and the API serve lives in dashboard/api-jobs.json instead.
data/erc8183/jobs.jsonl

# The published source repo is generated by scripts/publish-source.mjs from
# the mirror. It is deployed with the dashboard but never committed — a bare
# repository nested inside this one would confuse every git command run here.
dashboard/source.git/

# The operator personal identifiers used by the redaction gate. Local only:
# a rule that spells out the name it protects would publish that name in every
# copy of build-library.mjs.
scripts/lib/personal-identifiers.mjs

# Scratch file for the LP origin scan. Deliberately untracked; it was staged
# once by a blanket `git add -A`, which is what this line prevents.
lpscan-tmp.js

# Local archive of loose drafts, logs and old helpers moved out of the root on 2026-09-08. Never published.
archive/
